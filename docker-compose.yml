services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: takeley
      POSTGRES_PASSWORD: takeley
      POSTGRES_DB: takeley
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
